apiVersion: v1
kind: Service
metadata:
  name: support_ticket
  labels:
    app: support_ticket
spec:
  type: NodePort
  ports:
  - name: server
    port: 8087
    protocol: TCP
    targetPort: 8087
  selector:
    app: support_ticket
